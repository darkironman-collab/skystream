name: Energy Media Player Windows Build

on:
  push:
    branches:
      - energy-media-player
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: energy-media-player-windows-${{ github.ref }}
  cancel-in-progress: true

jobs:
  verify-and-build:
    name: Verify and build Windows x64
    runs-on: windows-2022
    timeout-minutes: 60

    steps:
      - name: Check out source
        uses: actions/checkout@v5

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate project files
        shell: pwsh
        run: |
          flutter config --enable-windows-desktop
          flutter pub get
          flutter gen-l10n
          dart run build_runner build --delete-conflicting-outputs

      - name: Format changed Dart files
        shell: pwsh
        run: dart format lib/core/services/external_player_service.dart test/core/services/external_player_service_test.dart

      - name: Analyze source
        shell: pwsh
        run: flutter analyze --no-fatal-warnings

      - name: Test extensions and external-player integration
        shell: pwsh
        run: flutter test test/core/extensions test/features/extensions test/core/services/external_player_service_test.dart test/compile_smoke_test.dart

      - name: Keep only the Windows TorrServer binary
        shell: pwsh
        run: |
          Set-Location packages/flutter_torrent_server/assets/torrserver
          Get-ChildItem -File | Where-Object { $_.Name -ne 'TorrServer-windows-amd64.exe' } | Remove-Item

      - name: Download libmpv
        shell: pwsh
        run: |
          choco install 7zip -y --no-progress
          $assetPattern = '^mpv-dev-x86_64-(?!v\d+-)\d{8}-git-[a-f0-9]+\.7z$'
          $release = Invoke-RestMethod -Uri 'https://api.github.com/repos/shinchiro/mpv-winbuild-cmake/releases/latest'
          $asset = $release.assets | Where-Object { $_.name -match $assetPattern } | Select-Object -First 1
          if (-not $asset) {
            throw "Could not find a libmpv asset matching $assetPattern"
          }

          $archivePath = Join-Path $env:RUNNER_TEMP 'mpv-dev.7z'
          $extractDir = Join-Path $env:RUNNER_TEMP 'mpv-dev-extract'
          Invoke-WebRequest -Uri $asset.browser_download_url -OutFile $archivePath
          New-Item -ItemType Directory -Path $extractDir -Force | Out-Null
          & 7z x $archivePath "-o$extractDir" -y | Out-Null

          $dll = Get-ChildItem -Path $extractDir -Filter 'libmpv-2.dll' -Recurse | Select-Object -First 1
          if (-not $dll) {
            throw 'libmpv-2.dll was not found in the downloaded archive'
          }
          "SKYSTREAM_WINDOWS_MPV_DLL=$($dll.FullName)" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Generate build configuration
        shell: bash
        run: |
          cat > dart-defines.json << 'JSON_EOF'
          {
            "TMDB_API_KEY": "${{ secrets.TMDB_API_KEY }}",
            "SIMKL_CLIENT_ID": "${{ secrets.SIMKL_CLIENT_ID }}",
            "SIMKL_CLIENT_SECRET": "${{ secrets.SIMKL_CLIENT_SECRET }}",
            "TRAKT_CLIENT_ID": "${{ secrets.TRAKT_CLIENT_ID }}",
            "TRAKT_CLIENT_SECRET": "${{ secrets.TRAKT_CLIENT_SECRET }}",
            "MAL_CLIENT_ID": "${{ secrets.MAL_CLIENT_ID }}",
            "MAL_CLIENT_SECRET": "${{ secrets.MAL_CLIENT_SECRET }}",
            "ANILIST_CLIENT_ID": "${{ secrets.ANILIST_CLIENT_ID }}",
            "ANIMESKIP_CLIENT_ID": "${{ secrets.ANIMESKIP_CLIENT_ID }}"
          }
          JSON_EOF

      - name: Build Windows release
        shell: pwsh
        run: flutter build windows --release --dart-define-from-file=dart-defines.json

      - name: Build installer and portable ZIP
        shell: pwsh
        run: |
          choco install innosetup -y --no-progress
          $appDir = '..\..\..\build\windows\x64\runner\Release'
          & 'C:\Program Files (x86)\Inno Setup 6\ISCC.exe' /DAppVersion=2.7.6-energy /DAppArch=x64 /DAppDir=$appDir windows\packaging\exe\setup.iss
          Compress-Archive -Path build\windows\x64\runner\Release\* -DestinationPath SkyStream-Energy-Player-Windows-x64-Portable.zip

      - name: Stage build outputs
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Path artifact -Force | Out-Null
          Copy-Item windows\packaging\exe\Output\*.exe artifact\
          Copy-Item SkyStream-Energy-Player-Windows-x64-Portable.zip artifact\

      - name: Upload build for malware scan
        uses: actions/upload-artifact@v4
        with:
          name: energy-player-windows-unscanned
          path: artifact/
          retention-days: 3

  malware-scan:
    name: ClamAV scan and checksums
    needs: verify-and-build
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Download Windows build
        uses: actions/download-artifact@v4
        with:
          name: energy-player-windows-unscanned
          path: artifact

      - name: Install and update ClamAV
        shell: bash
        run: |
          sudo apt-get update
          sudo apt-get install -y clamav
          sudo systemctl stop clamav-freshclam.service || true
          sudo freshclam

      - name: Scan and create SHA-256 checksums
        shell: bash
        working-directory: artifact
        run: |
          clamscan --recursive --infected --log=CLAMAV-SCAN.txt .
          sha256sum *.exe *.zip > SHA256SUMS.txt

      - name: Upload verified Windows build
        uses: actions/upload-artifact@v4
        with:
          name: SkyStream-Energy-Player-Windows-x64-verified
          path: artifact/
          retention-days: 7
